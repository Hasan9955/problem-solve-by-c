#include<bits/stdc++.h>
using namespace std;

int main () {

    string s;
 
    int n;

    cin >> n;

    // Those function are used to ignore the enter 
    cin.ignore();
    // getchar();

    getline(cin, s);

    cout << n << endl;
    cout << s << endl;

    cout << s.size() << endl;


    return 0;
}