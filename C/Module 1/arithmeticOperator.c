#include <stdio.h>
#include <math.h>

int main()
{

    // Addition
    // int a = 10;
    // int b = 20;

    // int c = a + b;

    // Division
    //  float a = 3, b = 20;

    // float dvd = b / a;

    // printf("Division of a and b is: %0.3f", dvd);

    // Multiplication
    // float a = 33.55, b = 26.55;

    // float mul = a * b;

    // printf("Multiplication of a and b is: %0.3f", mul);


    // Subtraction
    // int a = 33, b = 26;

    // int sub = a - b;

    // printf("Subtraction of a and b is: %d", sub);

    // Modulus
    int a = 25, b = 4;

    int module = a % b;

    printf("Modulus of a and b is: %d", module);

    return 0;


    //module decimal number
    // float a = 3.3, b = 26;

    // float module = fmodf(b, a);

    // printf("Modulus of a and b is: %f", module);

    // return 0;


    
}