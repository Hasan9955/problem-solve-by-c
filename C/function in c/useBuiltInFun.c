#include <stdio.h>
#include <math.h>
#include <stdlib.h>

int main () {

    // double x;
     

    // scanf("%lf", &x);

    // int ans = ceil(x);
    // int ans = floor(x);
    // int ans = round(x);

    // printf("%d", ans);



    // int a;

    // scanf("%d", &a);

    // double ans = sqrt(a);

    // printf("%lf", ans);



    // power function

    // int a, b;

    // scanf("%d %d", &a, &b);

    // int ans = pow(a, b);

    // printf("%d\n", ans);


    int x;
    scanf("%d", &x);

    int ans = abs(x);

    printf("%d", ans);
    return 0;
}